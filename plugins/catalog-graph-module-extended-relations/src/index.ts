export { catalogGraphModuleExtendedRelations as default } from './module';

/**
 * The entity-kind color palette used by the catalog graph, re-exported so
 * other UI (e.g. search result kind badges) can render a given kind the same
 * color everywhere in the app instead of inventing a second palette.
 */
export { getNodeColor, getNodeTintFill } from './graphUtils';
